# Agent Runtime

Planner/validator runtime that interprets natural-language requests and calls
the backend API.

This subfolder is intentionally separate from `backend/` so execution
guardrails remain isolated in the backend service.
